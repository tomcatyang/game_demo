## 第五步：拆分开发任务

### 目标
将产品需求文档（PRD）拆分为一系列结构化的开发任务，为后续实现阶段提供清晰的指导。

### 任务拆分流程

#### 1. 目录结构要求
- 在需求文档目录中创建 `tasks` 目录
- 将生成的任务清单文件放入该目录

#### 2. 输入内容
- 读取背景知识文档
``` javascript
let backKnowledgeDoc = ${docsDirPath}/背景知识.md
```

- 读取技术方案设计文档

``` javascript
let designDoc = ${docsDirPath}/方案设计文档.md
```
- 

###  任务结构定义
保持标准任务结构，使用中文描述任务信息：

```javascript
// Build the standard task structure
let taskStructure = `{
    "id": number,
    "title": string,
    "description": string,
    "status": "pending",
    "dependencies": number[] (IDs of tasks this depends on),
    "priority": "high" | "medium" | "low",
    "details": string (implementation details),
    "testStrategy": string (validation approach),
}`;
```

### 输出文档路径
- ${docsDirPath}/tasks/task.json

### 任务设计的核心原则

### **任务生成要求**
- **原子化**：每个任务专注单一职责
- **逻辑序列**：按依赖关系和实现流程排列
- **依赖约束**：任务只能依赖编号更小的任务
- **实现灵活性**：专注"做什么"而非"怎么做"

### **优先级指导**
- **基础优先**：早期任务设置核心功能
- **渐进复杂度**：后续高级功能和优化
- **优先级**：基于关键性和依赖位置（高/中/低）

### **依赖关系**
- 识别逻辑依赖，确保有向无环图
- 基础任务无依赖或少依赖
- 功能任务可能依赖基础任务
- 优化任务通常依赖功能任务

### **任务内容规则**

#### **title（任务标题）**
- **格式要求**：动词开头，简洁明确，不超过15字

#### **description（任务描述）**
- **内容要求**：说明任务目的和业务价值
- **格式**：1-2句话，回答"为什么需要这个任务"

#### **details（实现详情）**
- **核心内容**：
  - 实现方法和思路 **注意** 不要具体到代码细节
  - 涉及的目标文件和模块
  - 架构考虑和设计原则
  - 代码文件的作用和职责
- **格式要求**：
  - 分段落描述，逻辑清晰
  - 末尾统一添加："**注意**：不要过度设计，保持代码实现简洁"
- **示例**：
  ```
  在用户注册流程中添加邮箱验证功能。创建EmailValidator类处理验证逻辑，
  在UserService中集成验证调用。考虑验证失败的错误处理和用户提示。
  
  涉及文件：src/services/EmailValidator.ts, src/services/UserService.ts
  
  **注意**：不要过度设计代码，保持尽量简洁实现
  ```

#### **testStrategy（测试策略）**
- **内容要求**：
  - 验证方法和测试场景
  - 关键测试用例
  - 边界条件考虑
- **格式**：分点列出，具体可执行
